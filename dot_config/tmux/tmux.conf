# prefixをC-zに変更する
set -g prefix C-z

# C-z*2でtmux内のプログラムにC-zを送る
bind C-z send-prefix

# デフォルトのprefix(C-b)を解除する
unbind C-b

# マウス操作を有効にする
set -g mouse on

# ステータスラインをトップに配置する
set -g status-position top

# prefix+rで設定ファイルをリロードする
bind r source-file ~/.config/tmux/tmux.conf
